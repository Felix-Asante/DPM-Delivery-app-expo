export enum BookingStatus {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  DELIVERED = 'delivered',
  CANCELLED = 'cancelled',
  REJECTED = 'rejected',
}
export enum FILTERS {
  CHEAP_DELIVERY = 'Cheap delivery',
  SPECIAL_OFFERS = 'Special offers',
}
